# Sprint 3 test QA evidence — 2026-08-27

## Verdict

**PASS.** No release-blocking defect found in the Azure `test` SQL-persistence/idempotency scope.

## Release identity

- Resource group: `ordersapp-test`
- API endpoint: `https://cloudorders-test-api.gentlemoss-f04d5216.ukwest.azurecontainerapps.io`
- Live health: HTTPS `200 OK`; readiness was covered by successful create/read persistence operations.
- Latest, active revision: `cloudorders-test-api--0000002`.
- API image: `cloudorderst583431testacr.azurecr.io/cloudorders-api@sha256:60586ea283b8596c537ed996fc4f6cb91a96fac33a94c40fd1f7c45a86d46fad` (matches candidate digest).
- Migration execution `cloudorders-test-migrations-ux5fng5`: `Succeeded`.

## API test matrix

| Check | Result |
|---|---|
| Create order and retrieve it by returned ID | `201 Created`, then `200 OK`, same ID |
| Exact same idempotency-key replay | `200 OK`, `Idempotency-Replayed: true`, same ID |
| Semantically identical, reordered/whitespace JSON replay | `200 OK`, replay header, same ID |
| Same key with a different quantity | `409`, `idempotency_conflict` |
| Missing/invalid idempotency key | `400`, `invalid_idempotency_key` |
| Missing required field; zero quantity | `400`, `validation_error` |
| Malformed JSON; unsupported media type | `400` / `415`, `invalid_request` |
| Valid character/length boundary samples | accepted (`201`) |
| 16 simultaneous requests with one key and payload | one created result, 15 replays, one unique order ID |
| Recovery after 30 malformed requests | next valid create returned `201` |

## Direct SQL verification

Used a temporary exact-client-IP SQL firewall rule and Microsoft Entra token authentication. No connection strings, tokens, customer values, request hashes, or row contents were recorded. The rule was deleted and verified absent after each inspection.

- Migration history contains exactly `20260816221235_InitialSqlPersistence`.
- `Orders`, `OutboxMessages`, and `IdempotencyRecords` counts were each 5; no order lacked an outbox row, no idempotency row lacked its order, and no idempotency row lacked its outbox row.
- For the concurrent-request test key: exactly 1 idempotency record, 1 linked order, and 1 linked outbox message.
- Temporary firewall rules remaining after verification: `0`.

## Defects

None.
