# Sprint 4 Azure test QA — run 34323409707

**Date:** 2026-09-09
**Release:** test commit `db4c4b05a451a71247a52af055635f2c27aed837`
**Environment:** Azure `test` (`ordersapp-test`)
**Outcome:** Pass; no defects found.

## Deployment and smoke checks

GitHub Actions [run 34323409707](https://github.com/RobertMagowan/ordersApp/actions/runs/34323409707) completed successfully. The API revision was healthy and both anonymous probes returned `200`:

- `GET /health/live`
- `GET /health/ready`

Unauthenticated requests to protected endpoints returned `401`.

## Authenticated QA matrix

Two independently signed-in CIAM customer identities were used through an interactive PKCE runner. Tokens remained in memory and were neither printed nor stored.

- `/api/v1/me` returned `200` for each identity.
- Create and own-order read returned `201` and `200`.
- Exact idempotency replay returned `200`; a conflicting key reuse returned `409`.
- Missing or invalid idempotency keys, malformed JSON, invalid fields, and boundary values returned `400`.
- Unknown, malformed, and foreign order identifiers returned the safe `404` response.
- Concurrent same-key creates returned one `201` and one replay `200`.
- The second customer received `404` for an order that SQL confirmed belongs to the first customer.

## Persistence integrity

Read-only Entra-token SQL inspection confirmed three migrations and one QA-created row in each of `CustomerProfiles`, `Orders`, `IdempotencyRecords`, and `OutboxMessages`. There were no orders without a customer profile, no idempotency rows without actor or target profiles, and no orders without an idempotency record.

The test database was explicitly approved as disposable for this QA run.

## Commands (sanitized)

```powershell
dotnet run --project <temporary-qa-runner> -- --authority <deployed-authority> --client-id <deployed-client-id> --scope <orders-scope> --api-base-url <test-api-url>
curl.exe -sS -o NUL -w '<path> %{http_code}' <test-api-url>/<path>
az account get-access-token --resource https://database.windows.net/
Invoke-Sqlcmd -ServerInstance <test-sql-server> -Database CloudOrders -AccessToken <in-memory-token> -Query <read-only-integrity-query>
```
