name: Deploy CloudOrders MVP

on:
  push:
    branches:
      - development
      - test
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  validate_promotion_ref:
    name: Validate promotion ref
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.promotion_ref.outputs.environment }}
      migration_only: ${{ steps.e1_manifest.outputs.migration_only }}
      migration: ${{ steps.e1_manifest.outputs.migration }}
    steps:
      - name: Reject a non-promotion branch
        id: promotion_ref
        shell: bash
        run: |
          set -euo pipefail
          [[ "$GITHUB_REF_TYPE" == "branch" ]] || {
            echo "Manual deployments must target a branch, not $GITHUB_REF_TYPE '$GITHUB_REF_NAME'." >&2
            exit 1
          }
          case "$GITHUB_REF_NAME" in
            development|test)
              echo "environment=$GITHUB_REF_NAME" >> "$GITHUB_OUTPUT"
              ;;
            master)
              echo "environment=production" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "Manual deployments are allowed only from development, test, or master (got $GITHUB_REF_NAME)." >&2
              exit 1
              ;;
          esac

      - name: Check out Sprint 4A E1 manifest
        if: github.event_name == 'push' && (github.ref_name == 'development' || github.ref_name == 'test')
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Validate Sprint 4A E1 migration-only manifest
        id: e1_manifest
        shell: bash
        run: |
          set -euo pipefail
          echo "migration_only=false" >> "$GITHUB_OUTPUT"
          echo "migration=" >> "$GITHUB_OUTPUT"
          [[ "$GITHUB_EVENT_NAME" == push && ( "$GITHUB_REF_NAME" == development || "$GITHUB_REF_NAME" == test ) ]] || exit 0

          MANIFEST='ops/releases/sprint-4a-e1-migration-only.json'
          [[ -f "$MANIFEST" ]] || exit 0
          python3 - "$MANIFEST" <<'PY'
          import json
          import sys

          with open(sys.argv[1], encoding="utf-8") as source:
              manifest = json.load(source)
          expected = {"migration": "AddCustomerProfileOwnershipExpand", "deployApi": False}
          if manifest != expected:
              raise SystemExit("Sprint 4A E1 manifest must contain only the named migration and deployApi=false.")
          PY
          echo "migration_only=true" >> "$GITHUB_OUTPUT"
          echo "migration=AddCustomerProfileOwnershipExpand" >> "$GITHUB_OUTPUT"

  preview_foundation:
    name: Preview ${{ github.ref_name }} foundation
    needs: validate_promotion_ref
    if: needs.validate_promotion_ref.outputs.migration_only != 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.validate_promotion_ref.outputs.environment }}
    permissions:
      contents: read
      id-token: write
    outputs:
      enabled: ${{ steps.deployment_state.outputs.enabled }}
      exists: ${{ steps.existing_release.outputs.exists }}
      previous_image: ${{ steps.existing_release.outputs.previous_image }}
      previous_revision: ${{ steps.existing_release.outputs.previous_revision }}
    env:
      AZURE_DEPLOYMENT_ENABLED: ${{ vars.AZURE_DEPLOYMENT_ENABLED }}
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
      AZURE_APP_NAME: ${{ vars.AZURE_APP_NAME }}
      AZURE_ACR_NAME: ${{ vars.AZURE_ACR_NAME }}
      AZURE_MANAGED_ENVIRONMENT_NAME: ${{ vars.AZURE_MANAGED_ENVIRONMENT_NAME }}
      AZURE_LOG_ANALYTICS_NAME: ${{ vars.AZURE_LOG_ANALYTICS_NAME }}
      AZURE_SQL_ADMIN_LOGIN: ${{ vars.AZURE_SQL_ADMIN_LOGIN }}
      AZURE_SQL_ADMIN_OBJECT_ID: ${{ vars.AZURE_SQL_ADMIN_OBJECT_ID }}
      AZURE_SQL_ADMIN_TENANT_ID: ${{ vars.AZURE_SQL_ADMIN_TENANT_ID }}
      EXTERNAL_IDENTITY_ENABLED: ${{ vars.EXTERNAL_IDENTITY_ENABLED }}
      EXTERNAL_IDENTITY_AUTHORITY: ${{ vars.EXTERNAL_IDENTITY_AUTHORITY }}
      EXTERNAL_IDENTITY_VALID_ISSUER: ${{ vars.EXTERNAL_IDENTITY_VALID_ISSUER }}
      EXTERNAL_IDENTITY_TENANT_ID: ${{ vars.EXTERNAL_IDENTITY_TENANT_ID }}
      EXTERNAL_IDENTITY_AUDIENCE: ${{ vars.EXTERNAL_IDENTITY_AUDIENCE }}
      EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS: ${{ vars.EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS }}
      DEPLOYMENT_ENVIRONMENT: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    steps:
      - name: Read deployment state
        id: deployment_state
        run: echo "enabled=$AZURE_DEPLOYMENT_ENABLED" >> "$GITHUB_OUTPUT"

      - name: Publish deferred deployment summary
        if: env.AZURE_DEPLOYMENT_ENABLED != 'true'
        run: |
          {
            echo "## CloudOrders deployment"
            echo
            printf -- '- Release: `%s`\n' "$GITHUB_SHA"
            echo "- Immutable API image: not published (Azure deployment is disabled)"
            echo "- API endpoint: not deployed (Azure deployment is disabled)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Skip until Azure environment is provisioned
        if: env.AZURE_DEPLOYMENT_ENABLED != 'true'
        run: |
          echo "Azure deployment is disabled for this environment."
          echo "Set AZURE_DEPLOYMENT_ENABLED=true after configuring the Azure OIDC identity and resource group."

      - name: Validate deployment settings
        if: env.AZURE_DEPLOYMENT_ENABLED == 'true'
        env:
          CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: |
          for value in AZURE_RESOURCE_GROUP AZURE_LOCATION AZURE_APP_NAME AZURE_ACR_NAME AZURE_MANAGED_ENVIRONMENT_NAME AZURE_LOG_ANALYTICS_NAME CLIENT_ID TENANT_ID SUBSCRIPTION_ID; do
            if [[ -z "${!value}" ]]; then
              echo "Missing required deployment setting: $value" >&2
              exit 1
            fi
          done

      - name: Check out source
        if: env.AZURE_DEPLOYMENT_ENABLED == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Sign in to Azure with OIDC
        if: env.AZURE_DEPLOYMENT_ENABLED == 'true'
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Install and validate Bicep
        if: env.AZURE_DEPLOYMENT_ENABLED == 'true'
        run: |
          az bicep install
          az bicep lint --file infra/main.bicep
          az bicep build --file infra/main.bicep

      - name: Validate nonproduction External ID settings
        if: env.AZURE_DEPLOYMENT_ENABLED == 'true'
        shell: bash
        run: |
          set -euo pipefail
          EXTERNAL_IDENTITY_ENABLED="${EXTERNAL_IDENTITY_ENABLED:-false}"
          if [[ "$DEPLOYMENT_ENVIRONMENT" == production ]]; then
            [[ "$EXTERNAL_IDENTITY_ENABLED" != true ]] || { echo "External ID is not enabled for production." >&2; exit 1; }
            exit 0
          fi
          [[ "$EXTERNAL_IDENTITY_ENABLED" == true ]] || { echo "External ID must be enabled for nonproduction deployment." >&2; exit 1; }
          for value in EXTERNAL_IDENTITY_AUTHORITY EXTERNAL_IDENTITY_VALID_ISSUER EXTERNAL_IDENTITY_TENANT_ID EXTERNAL_IDENTITY_AUDIENCE EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS; do
            [[ -n "${!value}" ]] || { echo "Missing required External ID setting: $value" >&2; exit 1; }
          done

      - name: Inspect existing release
        if: env.AZURE_DEPLOYMENT_ENABLED == 'true'
        id: existing_release
        shell: bash
        run: |
          set +e
          LOOKUP_ERROR=$(az containerapp show \
            --name "$AZURE_APP_NAME" \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --only-show-errors --output none 2>&1)
          LOOKUP_STATUS=$?
          set -e

          if [[ "$LOOKUP_STATUS" -eq 0 ]]; then
            PREVIOUS_REVISION=$(az containerapp show \
              --name "$AZURE_APP_NAME" \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --query properties.latestReadyRevisionName --output tsv)
            if [[ -z "$PREVIOUS_REVISION" ]]; then
              echo "The existing Container App has no ready revision to retain for rollback." >&2
              exit 1
            fi
            PREVIOUS_IMAGE=$(az containerapp revision show \
              --name "$AZURE_APP_NAME" \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --revision "$PREVIOUS_REVISION" \
              --query properties.template.containers[0].image --output tsv)
            if [[ -z "$PREVIOUS_IMAGE" ]]; then
              echo "The ready revision does not expose a rollback image." >&2
              exit 1
            fi
            echo "exists=true" >> "$GITHUB_OUTPUT"
          elif [[ "$LOOKUP_ERROR" == *"ResourceNotFound"* ]]; then
            PREVIOUS_IMAGE='none (first deployment)'
            PREVIOUS_REVISION='none (first deployment)'
            echo "exists=false" >> "$GITHUB_OUTPUT"
          else
            echo "$LOOKUP_ERROR" >&2
            exit "$LOOKUP_STATUS"
          fi

          echo "previous_image=$PREVIOUS_IMAGE" >> "$GITHUB_OUTPUT"
          echo "previous_revision=$PREVIOUS_REVISION" >> "$GITHUB_OUTPUT"

      - name: Preview first-deployment foundation
        if: env.AZURE_DEPLOYMENT_ENABLED == 'true'
          && steps.existing_release.outputs.exists != 'true'
        run: |
          PARAM_FILE="infra/environments/$DEPLOYMENT_ENVIRONMENT.bicepparam"
          az deployment group what-if \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --template-file infra/main.bicep \
            --parameters "$PARAM_FILE" \
              location="$AZURE_LOCATION" \
              environmentName="$DEPLOYMENT_ENVIRONMENT" \
              releaseId=bootstrap \
              appName="$AZURE_APP_NAME" \
              acrName="$AZURE_ACR_NAME" \
              managedEnvironmentName="$AZURE_MANAGED_ENVIRONMENT_NAME" \
              logAnalyticsName="$AZURE_LOG_ANALYTICS_NAME" \
              targetPort=80 \
              enableLivenessProbe=false \
              useAcr=false \
              createAcrPullRole=false \
              containerImage='mcr.microsoft.com/k8se/quickstart:latest' \
              externalIdentityEnabled="${EXTERNAL_IDENTITY_ENABLED:-false}" \
              externalIdentityAuthority="$EXTERNAL_IDENTITY_AUTHORITY" \
              externalIdentityValidIssuer="$EXTERNAL_IDENTITY_VALID_ISSUER" \
              externalIdentityTenantId="$EXTERNAL_IDENTITY_TENANT_ID" \
              externalIdentityAudience="$EXTERNAL_IDENTITY_AUDIENCE" \
              externalIdentityAllowedClientIds="$EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS" \
            --mode Incremental \
            --result-format FullResourcePayloads

      - name: Publish foundation preview summary
        if: always() && env.AZURE_DEPLOYMENT_ENABLED == 'true'
        env:
          EXISTS: ${{ steps.existing_release.outputs.exists }}
          PREVIOUS_IMAGE: ${{ steps.existing_release.outputs.previous_image }}
          PREVIOUS_REVISION: ${{ steps.existing_release.outputs.previous_revision }}
        run: |
          EXISTS="${EXISTS:-unavailable (inspection failed)}"
          PREVIOUS_IMAGE="${PREVIOUS_IMAGE:-unavailable (inspection failed)}"
          PREVIOUS_REVISION="${PREVIOUS_REVISION:-unavailable (inspection failed)}"
          {
            echo "## CloudOrders foundation preview"
            echo
            printf -- '- Release: `%s`\n' "$GITHUB_SHA"
            printf -- '- Existing Container App: `%s`\n' "$EXISTS"
            printf -- '- Rollback image: `%s`\n' "$PREVIOUS_IMAGE"
            printf -- '- Previous ready revision: `%s`\n' "$PREVIOUS_REVISION"
            echo "- Mutation: none; foundation provisioning is in the downstream preparation job"
          } >> "$GITHUB_STEP_SUMMARY"

  prepare_release:
    name: Prepare and preview ${{ github.ref_name }} release
    needs: [preview_foundation, validate_promotion_ref]
    if: needs.validate_promotion_ref.outputs.migration_only != 'true' && needs.preview_foundation.outputs.enabled == 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    permissions:
      contents: read
      id-token: write
    outputs:
      image: ${{ steps.image.outputs.image }}
      image_tag: ${{ steps.image.outputs.tag }}
      digest: ${{ steps.image.outputs.digest }}
      migration_image: ${{ steps.migration_image.outputs.image }}
      previous_image: ${{ steps.rollback_context.outputs.previous_image }}
      previous_revision: ${{ steps.rollback_context.outputs.previous_revision }}
    env:
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
      AZURE_APP_NAME: ${{ vars.AZURE_APP_NAME }}
      AZURE_ACR_NAME: ${{ vars.AZURE_ACR_NAME }}
      AZURE_MANAGED_ENVIRONMENT_NAME: ${{ vars.AZURE_MANAGED_ENVIRONMENT_NAME }}
      AZURE_LOG_ANALYTICS_NAME: ${{ vars.AZURE_LOG_ANALYTICS_NAME }}
      EXTERNAL_IDENTITY_ENABLED: ${{ vars.EXTERNAL_IDENTITY_ENABLED }}
      EXTERNAL_IDENTITY_AUTHORITY: ${{ vars.EXTERNAL_IDENTITY_AUTHORITY }}
      EXTERNAL_IDENTITY_VALID_ISSUER: ${{ vars.EXTERNAL_IDENTITY_VALID_ISSUER }}
      EXTERNAL_IDENTITY_TENANT_ID: ${{ vars.EXTERNAL_IDENTITY_TENANT_ID }}
      EXTERNAL_IDENTITY_AUDIENCE: ${{ vars.EXTERNAL_IDENTITY_AUDIENCE }}
      EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS: ${{ vars.EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS }}
      DEPLOYMENT_ENVIRONMENT: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
      DEPLOYMENT_NAME: cloudorders-${{ github.ref_name == 'master' && 'production' || github.ref_name }}-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Retain rollback context
        id: rollback_context
        env:
          PREVIOUS_IMAGE: ${{ needs.preview_foundation.outputs.previous_image }}
          PREVIOUS_REVISION: ${{ needs.preview_foundation.outputs.previous_revision }}
        run: |
          echo "previous_image=$PREVIOUS_IMAGE" >> "$GITHUB_OUTPUT"
          echo "previous_revision=$PREVIOUS_REVISION" >> "$GITHUB_OUTPUT"

      - name: Check out source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Sign in to Azure with OIDC
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Install Bicep
        run: az bicep install

      - name: Validate nonproduction External ID settings
        shell: bash
        run: |
          set -euo pipefail
          EXTERNAL_IDENTITY_ENABLED="${EXTERNAL_IDENTITY_ENABLED:-false}"
          if [[ "$DEPLOYMENT_ENVIRONMENT" == production ]]; then
            [[ "$EXTERNAL_IDENTITY_ENABLED" != true ]] || { echo "External ID is not enabled for production." >&2; exit 1; }
            exit 0
          fi
          [[ "$EXTERNAL_IDENTITY_ENABLED" == true ]] || { echo "External ID must be enabled for nonproduction deployment." >&2; exit 1; }
          for value in EXTERNAL_IDENTITY_AUTHORITY EXTERNAL_IDENTITY_VALID_ISSUER EXTERNAL_IDENTITY_TENANT_ID EXTERNAL_IDENTITY_AUDIENCE EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS; do
            [[ -n "${!value}" ]] || { echo "Missing required External ID setting: $value" >&2; exit 1; }
          done

      - name: Provision MVP foundation with public bootstrap image
        if: needs.preview_foundation.outputs.exists != 'true'
        run: |
          az deployment group create \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --name "$DEPLOYMENT_NAME-bootstrap" \
            --template-file infra/main.bicep \
            --parameters \
              "infra/environments/$DEPLOYMENT_ENVIRONMENT.bicepparam" \
              location="$AZURE_LOCATION" \
              environmentName="$DEPLOYMENT_ENVIRONMENT" \
              releaseId=bootstrap \
              appName="$AZURE_APP_NAME" \
              acrName="$AZURE_ACR_NAME" \
              managedEnvironmentName="$AZURE_MANAGED_ENVIRONMENT_NAME" \
              logAnalyticsName="$AZURE_LOG_ANALYTICS_NAME" \
              targetPort=80 \
              enableLivenessProbe=false \
              useAcr=false \
              createAcrPullRole=false \
              containerImage='mcr.microsoft.com/k8se/quickstart:latest' \
              externalIdentityEnabled="${EXTERNAL_IDENTITY_ENABLED:-false}" \
              externalIdentityAuthority="$EXTERNAL_IDENTITY_AUTHORITY" \
              externalIdentityValidIssuer="$EXTERNAL_IDENTITY_VALID_ISSUER" \
              externalIdentityTenantId="$EXTERNAL_IDENTITY_TENANT_ID" \
              externalIdentityAudience="$EXTERNAL_IDENTITY_AUDIENCE" \
              externalIdentityAllowedClientIds="$EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS" \
            --mode Incremental

      - name: Build and publish immutable API image
        id: image
        run: |
          LOGIN_SERVER=$(az acr show --name "$AZURE_ACR_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query loginServer --output tsv)
          az acr login --name "$AZURE_ACR_NAME"
          IMAGE_TAG="$LOGIN_SERVER/cloudorders-api:${GITHUB_SHA}"
          docker build --file src/CloudOrders.Api/Dockerfile --tag "$IMAGE_TAG" .
          docker push "$IMAGE_TAG"
          DIGEST=$(az acr repository show --name "$AZURE_ACR_NAME" --image "cloudorders-api:${GITHUB_SHA}" --query digest --output tsv)
          if [[ -z "$DIGEST" ]]; then
            echo "Could not resolve the pushed image digest." >&2
            exit 1
          fi
          IMAGE="$LOGIN_SERVER/cloudorders-api@$DIGEST"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"
          echo "tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Build and publish immutable migration image
        id: migration_image
        run: |
          LOGIN_SERVER=$(az acr show --name "$AZURE_ACR_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query loginServer --output tsv)
          IMAGE_TAG="$LOGIN_SERVER/cloudorders-migrations:${GITHUB_SHA}"
          docker build --file src/CloudOrders.Migrations/Dockerfile --tag "$IMAGE_TAG" .
          docker push "$IMAGE_TAG"
          DIGEST=$(az acr repository show --name "$AZURE_ACR_NAME" --image "cloudorders-migrations:${GITHUB_SHA}" --query digest --output tsv)
          [[ -n "$DIGEST" ]] || { echo "Could not resolve the migration image digest." >&2; exit 1; }
          echo "image=$LOGIN_SERVER/cloudorders-migrations@$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Grant managed identity ACR pull access
        run: |
          PRINCIPAL_ID=$(az containerapp show \
            --name "$AZURE_APP_NAME" \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --query identity.principalId --output tsv)
          REGISTRY_ID=$(az acr show \
            --name "$AZURE_ACR_NAME" \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --query id --output tsv)
          EXISTING=$(az role assignment list \
            --assignee-object-id "$PRINCIPAL_ID" \
            --scope "$REGISTRY_ID" \
            --query "[?roleDefinitionName=='AcrPull'].id | [0]" --output tsv)
          if [[ -z "$EXISTING" ]]; then
            az role assignment create \
              --assignee-object-id "$PRINCIPAL_ID" \
              --assignee-principal-type ServicePrincipal \
              --role AcrPull \
              --scope "$REGISTRY_ID"
          fi

      - name: Preview immutable release
        env:
          IMAGE: ${{ steps.image.outputs.image }}
        run: |
          PARAM_FILE="infra/environments/$DEPLOYMENT_ENVIRONMENT.bicepparam"
          az deployment group what-if \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --template-file infra/main.bicep \
            --parameters "$PARAM_FILE" \
              location="$AZURE_LOCATION" \
              environmentName="$DEPLOYMENT_ENVIRONMENT" \
              releaseId="$GITHUB_SHA" \
              appName="$AZURE_APP_NAME" \
              acrName="$AZURE_ACR_NAME" \
              managedEnvironmentName="$AZURE_MANAGED_ENVIRONMENT_NAME" \
              logAnalyticsName="$AZURE_LOG_ANALYTICS_NAME" \
              targetPort=8080 \
              enableLivenessProbe=true \
              useAcr=true \
              createAcrPullRole=false \
              containerImage="$IMAGE" \
              deployMigrationJob=false \
              externalIdentityEnabled="${EXTERNAL_IDENTITY_ENABLED:-false}" \
              externalIdentityAuthority="$EXTERNAL_IDENTITY_AUTHORITY" \
              externalIdentityValidIssuer="$EXTERNAL_IDENTITY_VALID_ISSUER" \
              externalIdentityTenantId="$EXTERNAL_IDENTITY_TENANT_ID" \
              externalIdentityAudience="$EXTERNAL_IDENTITY_AUDIENCE" \
              externalIdentityAllowedClientIds="$EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS" \
            --mode Incremental \
            --result-format FullResourcePayloads

      - name: Publish immutable release preview summary
        if: always()
        env:
          IMAGE: ${{ steps.image.outputs.image }}
          PREVIOUS_IMAGE: ${{ needs.preview_foundation.outputs.previous_image }}
          PREVIOUS_REVISION: ${{ needs.preview_foundation.outputs.previous_revision }}
        run: |
          IMAGE="${IMAGE:-unavailable (preparation failed)}"
          PREVIOUS_IMAGE="${PREVIOUS_IMAGE:-unavailable (inspection failed)}"
          PREVIOUS_REVISION="${PREVIOUS_REVISION:-unavailable (inspection failed)}"
          {
            echo "## CloudOrders immutable release preview"
            echo
            printf -- '- Release: `%s`\n' "$GITHUB_SHA"
            printf -- '- Candidate image: `%s`\n' "$IMAGE"
            printf -- '- Rollback image: `%s`\n' "$PREVIOUS_IMAGE"
            printf -- '- Previous ready revision: `%s`\n' "$PREVIOUS_REVISION"
            echo "- Release mutation: none; deployment is in the downstream environment job"
          } >> "$GITHUB_STEP_SUMMARY"

  preview_sql:
    name: Preview ${{ github.ref_name }} Azure SQL
    needs: [prepare_release, validate_promotion_ref]
    if: needs.validate_promotion_ref.outputs.migration_only != 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    permissions:
      contents: read
      id-token: write
    env:
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
      AZURE_APP_NAME: ${{ vars.AZURE_APP_NAME }}
      AZURE_ACR_NAME: ${{ vars.AZURE_ACR_NAME }}
      AZURE_MANAGED_ENVIRONMENT_NAME: ${{ vars.AZURE_MANAGED_ENVIRONMENT_NAME }}
      AZURE_LOG_ANALYTICS_NAME: ${{ vars.AZURE_LOG_ANALYTICS_NAME }}
      AZURE_SQL_ADMIN_LOGIN: ${{ vars.AZURE_SQL_ADMIN_LOGIN }}
      AZURE_SQL_ADMIN_OBJECT_ID: ${{ vars.AZURE_SQL_ADMIN_OBJECT_ID }}
      AZURE_SQL_ADMIN_TENANT_ID: ${{ vars.AZURE_SQL_ADMIN_TENANT_ID }}
      DEPLOYMENT_ENVIRONMENT: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    steps:
      - name: Check out source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Sign in to Azure with OIDC
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Preview nonproduction Azure SQL
        env:
          API_IMAGE: ${{ needs.prepare_release.outputs.image }}
          MIGRATION_IMAGE: ${{ needs.prepare_release.outputs.migration_image }}
        run: |
          if [[ "$DEPLOYMENT_ENVIRONMENT" == production ]]; then
            echo "Sprint 3 Azure SQL is intentionally not enabled for production."
            exit 0
          fi
          for value in AZURE_SQL_ADMIN_LOGIN AZURE_SQL_ADMIN_OBJECT_ID AZURE_SQL_ADMIN_TENANT_ID; do
            [[ -n "${!value}" ]] || { echo "Missing required SQL setting: $value" >&2; exit 1; }
          done
          az deployment group what-if --resource-group "$AZURE_RESOURCE_GROUP" --template-file infra/main.bicep --parameters "infra/environments/$DEPLOYMENT_ENVIRONMENT.bicepparam" location="$AZURE_LOCATION" environmentName="$DEPLOYMENT_ENVIRONMENT" releaseId="$GITHUB_SHA" appName="$AZURE_APP_NAME" acrName="$AZURE_ACR_NAME" managedEnvironmentName="$AZURE_MANAGED_ENVIRONMENT_NAME" logAnalyticsName="$AZURE_LOG_ANALYTICS_NAME" containerImage="$API_IMAGE" migrationImage="$MIGRATION_IMAGE" useAcr=true deploySql=true deployContainerApp=false deployMigrationJob=false sqlAdministratorLogin="$AZURE_SQL_ADMIN_LOGIN" sqlAdministratorObjectId="$AZURE_SQL_ADMIN_OBJECT_ID" sqlAdministratorTenantId="$AZURE_SQL_ADMIN_TENANT_ID" --mode Incremental --result-format FullResourcePayloads

  bootstrap_sql:
    name: Provision ${{ github.ref_name }} Azure SQL
    needs: [preview_sql, prepare_release, validate_promotion_ref]
    if: needs.validate_promotion_ref.outputs.migration_only != 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    permissions:
      contents: read
      id-token: write
    env:
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
      AZURE_APP_NAME: ${{ vars.AZURE_APP_NAME }}
      AZURE_ACR_NAME: ${{ vars.AZURE_ACR_NAME }}
      AZURE_MANAGED_ENVIRONMENT_NAME: ${{ vars.AZURE_MANAGED_ENVIRONMENT_NAME }}
      AZURE_LOG_ANALYTICS_NAME: ${{ vars.AZURE_LOG_ANALYTICS_NAME }}
      AZURE_SQL_ADMIN_LOGIN: ${{ vars.AZURE_SQL_ADMIN_LOGIN }}
      AZURE_SQL_ADMIN_OBJECT_ID: ${{ vars.AZURE_SQL_ADMIN_OBJECT_ID }}
      AZURE_SQL_ADMIN_TENANT_ID: ${{ vars.AZURE_SQL_ADMIN_TENANT_ID }}
      DEPLOYMENT_ENVIRONMENT: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    steps:
      - name: Check out source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Sign in to Azure with OIDC
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Provision nonproduction Azure SQL
        env:
          API_IMAGE: ${{ needs.prepare_release.outputs.image }}
          MIGRATION_IMAGE: ${{ needs.prepare_release.outputs.migration_image }}
        run: |
          if [[ "$DEPLOYMENT_ENVIRONMENT" == production ]]; then
            echo "Sprint 3 Azure SQL is intentionally not enabled for production."
            exit 0
          fi
          SQL_DEPLOYMENT_NAME="cloudorders-$DEPLOYMENT_ENVIRONMENT-sql-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          az deployment group create --resource-group "$AZURE_RESOURCE_GROUP" --name "$SQL_DEPLOYMENT_NAME-identity" --template-file infra/main.bicep --parameters "infra/environments/$DEPLOYMENT_ENVIRONMENT.bicepparam" location="$AZURE_LOCATION" environmentName="$DEPLOYMENT_ENVIRONMENT" releaseId="$GITHUB_SHA" appName="$AZURE_APP_NAME" acrName="$AZURE_ACR_NAME" managedEnvironmentName="$AZURE_MANAGED_ENVIRONMENT_NAME" logAnalyticsName="$AZURE_LOG_ANALYTICS_NAME" containerImage="$API_IMAGE" migrationImage="$MIGRATION_IMAGE" useAcr=true deploySql=true deployContainerApp=false deployMigrationJob=false sqlAdministratorLogin="$AZURE_SQL_ADMIN_LOGIN" sqlAdministratorObjectId="$AZURE_SQL_ADMIN_OBJECT_ID" sqlAdministratorTenantId="$AZURE_SQL_ADMIN_TENANT_ID" --mode Incremental
          for attempt in {1..5}; do
            retry_output=$(mktemp)
            set +e
            az deployment group create --resource-group "$AZURE_RESOURCE_GROUP" --name "$SQL_DEPLOYMENT_NAME-job-$attempt" --template-file infra/main.bicep --parameters "infra/environments/$DEPLOYMENT_ENVIRONMENT.bicepparam" location="$AZURE_LOCATION" environmentName="$DEPLOYMENT_ENVIRONMENT" releaseId="$GITHUB_SHA" appName="$AZURE_APP_NAME" acrName="$AZURE_ACR_NAME" managedEnvironmentName="$AZURE_MANAGED_ENVIRONMENT_NAME" logAnalyticsName="$AZURE_LOG_ANALYTICS_NAME" containerImage="$API_IMAGE" migrationImage="$MIGRATION_IMAGE" useAcr=true deploySql=true deployContainerApp=false deployMigrationJob=true sqlAdministratorLogin="$AZURE_SQL_ADMIN_LOGIN" sqlAdministratorObjectId="$AZURE_SQL_ADMIN_OBJECT_ID" sqlAdministratorTenantId="$AZURE_SQL_ADMIN_TENANT_ID" --mode Incremental 2>&1 | tee "$retry_output"
            deployment_status=${PIPESTATUS[0]}
            set -e
            if [[ "$deployment_status" -eq 0 ]]; then
              break
            fi
            if ! grep -qiF 'InvalidParameterValueInContainerTemplate' "$retry_output" || ! grep -qiF 'unable to pull image' "$retry_output" || [[ "$attempt" -eq 5 ]]; then
              exit "$deployment_status"
            fi
            sleep $((attempt * 15))
          done

  run_migration:
    name: Run ${{ github.ref_name }} SQL migration
    needs: [bootstrap_sql, validate_promotion_ref]
    if: needs.validate_promotion_ref.outputs.migration_only != 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    permissions:
      contents: read
      id-token: write
    env:
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
      DEPLOYMENT_ENVIRONMENT: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    steps:
      - name: Sign in to Azure with OIDC
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Start nonproduction migration Job
        run: |
          if [[ "$DEPLOYMENT_ENVIRONMENT" == production ]]; then
            echo "No Sprint 3 migration job is configured for production."
            exit 0
          fi
          case "$DEPLOYMENT_ENVIRONMENT" in
            development) JOB_NAME='cloudorders-dev-migrations' ;;
            test) JOB_NAME='cloudorders-test-migrations' ;;
            *) echo "Unsupported migration environment: $DEPLOYMENT_ENVIRONMENT" >&2; exit 1 ;;
          esac
          EXECUTION=$(az containerapp job start --name "$JOB_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query name --output tsv)
          [[ -n "$EXECUTION" ]] || { echo "Migration Job did not return an execution name." >&2; exit 1; }
          for _ in {1..30}; do
            STATUS=$(az containerapp job execution show --name "$JOB_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --job-execution-name "$EXECUTION" --query properties.status --output tsv)
            [[ "$STATUS" == Succeeded ]] && exit 0
            [[ "$STATUS" == Failed ]] && { echo "Migration Job execution $EXECUTION failed." >&2; exit 1; }
            sleep 10
          done
          echo "Migration Job did not complete within five minutes." >&2
          exit 1

  run_sprint_4a_e1_migration_only:
    name: Run Sprint 4A E1 migration only
    needs: validate_promotion_ref
    if: needs.validate_promotion_ref.outputs.migration_only == 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.validate_promotion_ref.outputs.environment }}
    permissions:
      contents: read
      id-token: write
    env:
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
      AZURE_APP_NAME: ${{ vars.AZURE_APP_NAME }}
      AZURE_ACR_NAME: ${{ vars.AZURE_ACR_NAME }}
      DEPLOYMENT_ENVIRONMENT: ${{ github.ref_name }}
      MIGRATION: ${{ needs.validate_promotion_ref.outputs.migration }}
    steps:
      - name: Check out E1 migration source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Sign in to Azure with OIDC
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Build and publish immutable E1 migration image
        id: e1_migration_image
        shell: bash
        run: |
          set -euo pipefail
          LOGIN_SERVER=$(az acr show --name "$AZURE_ACR_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query loginServer --output tsv)
          [[ -n "$LOGIN_SERVER" ]] || { echo "Could not resolve the Azure Container Registry login server." >&2; exit 1; }
          az acr login --name "$AZURE_ACR_NAME"
          IMAGE_TAG="$LOGIN_SERVER/cloudorders-migrations:${GITHUB_SHA}"
          docker build --file src/CloudOrders.Migrations/Dockerfile --tag "$IMAGE_TAG" .
          docker push "$IMAGE_TAG"
          DIGEST=$(az acr repository show --name "$AZURE_ACR_NAME" --image "cloudorders-migrations:${GITHUB_SHA}" --query digest --output tsv)
          [[ -n "$DIGEST" ]] || { echo "Could not resolve the pushed E1 migration image digest." >&2; exit 1; }
          echo "image=$LOGIN_SERVER/cloudorders-migrations@$DIGEST" >> "$GITHUB_OUTPUT"
      - name: Start and verify only the named E1 migration execution
        shell: bash
        env:
          MIGRATION_IMAGE: ${{ steps.e1_migration_image.outputs.image }}
        run: |
          set -euo pipefail
          case "$DEPLOYMENT_ENVIRONMENT" in
            development) JOB_NAME='cloudorders-dev-migrations' ;;
            test) JOB_NAME='cloudorders-test-migrations' ;;
            *) echo "Sprint 4A E1 is restricted to development and test." >&2; exit 1 ;;
          esac

          BEFORE_REVISION=$(az containerapp show --name "$AZURE_APP_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query properties.latestRevisionName --output tsv)
          BEFORE_TRAFFIC=$(az containerapp show --name "$AZURE_APP_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query properties.configuration.ingress.traffic --output json)
          BEFORE_DIGEST=$(az containerapp revision show --name "$AZURE_APP_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --revision "$BEFORE_REVISION" --query properties.template.containers[0].image --output tsv)
          [[ -n "$BEFORE_REVISION" && -n "$BEFORE_DIGEST" ]] || { echo "Migration-only release requires an active API revision and digest." >&2; exit 1; }

          EXECUTION=$(az containerapp job start --name "$JOB_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --image "$MIGRATION_IMAGE" --args "--migration" "$MIGRATION" --query name --output tsv)
          [[ -n "$EXECUTION" ]] || { echo "Migration Job did not return an execution name." >&2; exit 1; }
          EXECUTION_ARGS=$(az containerapp job execution show --name "$JOB_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --job-execution-name "$EXECUTION" --query 'properties.template.containers[0].args' --output json)
          EXECUTION_IMAGE=$(az containerapp job execution show --name "$JOB_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --job-execution-name "$EXECUTION" --query 'properties.template.containers[0].image' --output tsv)
          python3 - "$MIGRATION" "$EXECUTION_ARGS" "$EXECUTION_IMAGE" "$MIGRATION_IMAGE" <<'PY'
          import json
          import sys

          expected = ["--migration", sys.argv[1]]
          if json.loads(sys.argv[2]) != expected:
              raise SystemExit("Migration execution did not receive exactly the manifest's named migration arguments.")
          if sys.argv[3] != sys.argv[4]:
              raise SystemExit("Migration execution did not use the current immutable migration image.")
          PY
          for _ in {1..30}; do
            STATUS=$(az containerapp job execution show --name "$JOB_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --job-execution-name "$EXECUTION" --query properties.status --output tsv)
            [[ "$STATUS" == Succeeded ]] && break
            [[ "$STATUS" == Failed ]] && { echo "Migration Job execution $EXECUTION failed." >&2; exit 1; }
            sleep 10
          done
          [[ "$STATUS" == Succeeded ]] || { echo "Migration Job did not complete within five minutes." >&2; exit 1; }

          AFTER_REVISION=$(az containerapp show --name "$AZURE_APP_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query properties.latestRevisionName --output tsv)
          AFTER_TRAFFIC=$(az containerapp show --name "$AZURE_APP_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query properties.configuration.ingress.traffic --output json)
          AFTER_DIGEST=$(az containerapp revision show --name "$AZURE_APP_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --revision "$AFTER_REVISION" --query properties.template.containers[0].image --output tsv)
          [[ "$BEFORE_REVISION" == "$AFTER_REVISION" && "$BEFORE_DIGEST" == "$AFTER_DIGEST" && "$BEFORE_TRAFFIC" == "$AFTER_TRAFFIC" ]] || {
            echo "Migration-only run changed API revision, digest, or traffic." >&2
            exit 1
          }

  deploy_release:
    name: Deploy ${{ github.ref_name }} after preview
    needs: [prepare_release, run_migration, validate_promotion_ref]
    if: needs.validate_promotion_ref.outputs.migration_only != 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
    permissions:
      contents: read
      id-token: write
    env:
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
      AZURE_APP_NAME: ${{ vars.AZURE_APP_NAME }}
      AZURE_ACR_NAME: ${{ vars.AZURE_ACR_NAME }}
      AZURE_MANAGED_ENVIRONMENT_NAME: ${{ vars.AZURE_MANAGED_ENVIRONMENT_NAME }}
      AZURE_LOG_ANALYTICS_NAME: ${{ vars.AZURE_LOG_ANALYTICS_NAME }}
      AZURE_SQL_ADMIN_LOGIN: ${{ vars.AZURE_SQL_ADMIN_LOGIN }}
      AZURE_SQL_ADMIN_OBJECT_ID: ${{ vars.AZURE_SQL_ADMIN_OBJECT_ID }}
      AZURE_SQL_ADMIN_TENANT_ID: ${{ vars.AZURE_SQL_ADMIN_TENANT_ID }}
      EXTERNAL_IDENTITY_ENABLED: ${{ vars.EXTERNAL_IDENTITY_ENABLED }}
      EXTERNAL_IDENTITY_AUTHORITY: ${{ vars.EXTERNAL_IDENTITY_AUTHORITY }}
      EXTERNAL_IDENTITY_VALID_ISSUER: ${{ vars.EXTERNAL_IDENTITY_VALID_ISSUER }}
      EXTERNAL_IDENTITY_TENANT_ID: ${{ vars.EXTERNAL_IDENTITY_TENANT_ID }}
      EXTERNAL_IDENTITY_AUDIENCE: ${{ vars.EXTERNAL_IDENTITY_AUDIENCE }}
      EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS: ${{ vars.EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS }}
      DEPLOYMENT_ENVIRONMENT: ${{ github.ref_name == 'master' && 'production' || github.ref_name }}
      DEPLOYMENT_NAME: cloudorders-${{ github.ref_name == 'master' && 'production' || github.ref_name }}-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Check out source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Sign in to Azure with OIDC
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Install Bicep
        run: az bicep install

      - name: Validate nonproduction External ID settings
        shell: bash
        run: |
          set -euo pipefail
          EXTERNAL_IDENTITY_ENABLED="${EXTERNAL_IDENTITY_ENABLED:-false}"
          if [[ "$DEPLOYMENT_ENVIRONMENT" == production ]]; then
            [[ "$EXTERNAL_IDENTITY_ENABLED" != true ]] || { echo "External ID is not enabled for production." >&2; exit 1; }
            exit 0
          fi
          [[ "$EXTERNAL_IDENTITY_ENABLED" == true ]] || { echo "External ID must be enabled for nonproduction deployment." >&2; exit 1; }
          for value in EXTERNAL_IDENTITY_AUTHORITY EXTERNAL_IDENTITY_VALID_ISSUER EXTERNAL_IDENTITY_TENANT_ID EXTERNAL_IDENTITY_AUDIENCE EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS; do
            [[ -n "${!value}" ]] || { echo "Missing required External ID setting: $value" >&2; exit 1; }
          done

      - name: Deploy immutable image after preview
        env:
          IMAGE: ${{ needs.prepare_release.outputs.image }}
        run: |
          DEPLOY_SQL=false
          if [[ "$DEPLOYMENT_ENVIRONMENT" != production ]]; then
            DEPLOY_SQL=true
          fi
          az deployment group create \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --name "$DEPLOYMENT_NAME" \
            --template-file infra/main.bicep \
            --parameters \
              "infra/environments/$DEPLOYMENT_ENVIRONMENT.bicepparam" \
              location="$AZURE_LOCATION" \
              environmentName="$DEPLOYMENT_ENVIRONMENT" \
              releaseId="$GITHUB_SHA" \
              appName="$AZURE_APP_NAME" \
              acrName="$AZURE_ACR_NAME" \
              managedEnvironmentName="$AZURE_MANAGED_ENVIRONMENT_NAME" \
              logAnalyticsName="$AZURE_LOG_ANALYTICS_NAME" \
              targetPort=8080 \
              enableLivenessProbe=true \
              useAcr=true \
              createAcrPullRole=false \
              containerImage="$IMAGE" \
              deploySql="$DEPLOY_SQL" \
              deployMigrationJob=false \
              sqlAdministratorLogin="$AZURE_SQL_ADMIN_LOGIN" \
              sqlAdministratorObjectId="$AZURE_SQL_ADMIN_OBJECT_ID" \
              sqlAdministratorTenantId="$AZURE_SQL_ADMIN_TENANT_ID" \
              externalIdentityEnabled="${EXTERNAL_IDENTITY_ENABLED:-false}" \
              externalIdentityAuthority="$EXTERNAL_IDENTITY_AUTHORITY" \
              externalIdentityValidIssuer="$EXTERNAL_IDENTITY_VALID_ISSUER" \
              externalIdentityTenantId="$EXTERNAL_IDENTITY_TENANT_ID" \
              externalIdentityAudience="$EXTERNAL_IDENTITY_AUDIENCE" \
              externalIdentityAllowedClientIds="$EXTERNAL_IDENTITY_ALLOWED_CLIENT_IDS" \
            --mode Incremental

      - name: Wait for candidate revision
        id: candidate
        env:
          IMAGE: ${{ needs.prepare_release.outputs.image }}
        run: |
          EXPECTED_IMAGE="$IMAGE"
          CANDIDATE_READY=false
          for attempt in {1..30}; do
            APP_DETAILS=$(az containerapp show \
              --name "$AZURE_APP_NAME" \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --query '{latestRevision:properties.latestRevisionName,latestReadyRevision:properties.latestReadyRevisionName,release:tags.release}' \
              --output json)
            CANDIDATE_REVISION=$(printf '%s' "$APP_DETAILS" | jq --raw-output '.latestRevision // empty')
            LATEST_READY_REVISION=$(printf '%s' "$APP_DETAILS" | jq --raw-output '.latestReadyRevision // empty')
            APP_RELEASE=$(printf '%s' "$APP_DETAILS" | jq --raw-output '.release // empty')

            if [[ -z "$CANDIDATE_REVISION" ]]; then
              echo "No candidate revision is reported on attempt $attempt; waiting."
              sleep 10
              continue
            fi

            if ! REVISION_DETAILS=$(az containerapp revision show \
              --name "$AZURE_APP_NAME" \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --revision "$CANDIDATE_REVISION" \
              --query '{provisioningState:properties.provisioningState,runningState:properties.runningState,healthState:properties.healthState,trafficWeight:properties.trafficWeight,image:properties.template.containers[0].image}' \
              --output json); then
              echo "Candidate revision $CANDIDATE_REVISION is not queryable on attempt $attempt; waiting."
              sleep 10
              continue
            fi

            CANDIDATE_IMAGE=$(printf '%s' "$REVISION_DETAILS" | jq --raw-output '.image // empty')
            CANDIDATE_PROVISIONING_STATE=$(printf '%s' "$REVISION_DETAILS" | jq --raw-output '.provisioningState // empty')
            CANDIDATE_RUNNING_STATE=$(printf '%s' "$REVISION_DETAILS" | jq --raw-output '.runningState // empty')
            CANDIDATE_HEALTH_STATE=$(printf '%s' "$REVISION_DETAILS" | jq --raw-output '.healthState // empty')
            CANDIDATE_TRAFFIC_WEIGHT=$(printf '%s' "$REVISION_DETAILS" | jq --raw-output '.trafficWeight // empty')

            if [[ "$CANDIDATE_IMAGE" == "$EXPECTED_IMAGE" && \
              "$CANDIDATE_PROVISIONING_STATE" == "Provisioned" && \
              "$CANDIDATE_RUNNING_STATE" == "Running" && \
              "$CANDIDATE_HEALTH_STATE" == "Healthy" && \
              "$CANDIDATE_TRAFFIC_WEIGHT" == "100" && \
              "$LATEST_READY_REVISION" == "$CANDIDATE_REVISION" && \
              "$APP_RELEASE" == "$GITHUB_SHA" ]]; then
              echo "Candidate revision $CANDIDATE_REVISION is the ready release for $EXPECTED_IMAGE."
              echo "revision=$CANDIDATE_REVISION" >> "$GITHUB_OUTPUT"
              echo "image=$CANDIDATE_IMAGE" >> "$GITHUB_OUTPUT"
              CANDIDATE_READY=true
              break
            fi

            echo "Candidate $CANDIDATE_REVISION on attempt $attempt: image=$CANDIDATE_IMAGE provisioning=$CANDIDATE_PROVISIONING_STATE running=$CANDIDATE_RUNNING_STATE health=$CANDIDATE_HEALTH_STATE traffic=$CANDIDATE_TRAFFIC_WEIGHT latestReady=$LATEST_READY_REVISION release=$APP_RELEASE; waiting."
            sleep 10
          done
          if [[ "$CANDIDATE_READY" != true ]]; then
            echo "Candidate revision did not become the ready 100% traffic release for $EXPECTED_IMAGE." >&2
            exit 1
          fi

      - name: Smoke test the deployed API
        id: endpoint
        env:
          CANDIDATE_REVISION: ${{ steps.candidate.outputs.revision }}
          CANDIDATE_IMAGE: ${{ steps.candidate.outputs.image }}
        run: |
          FQDN=$(az containerapp show --name "$AZURE_APP_NAME" --resource-group "$AZURE_RESOURCE_GROUP" --query properties.configuration.ingress.fqdn --output tsv)
          ENDPOINT="https://$FQDN"
          HEALTHY=false
          echo "API endpoint: $ENDPOINT"
          for attempt in {1..30}; do
            STATUS=$(curl --fail --silent --show-error --output /dev/null --write-out '%{http_code}' "$ENDPOINT/health/live" || true)
            if [[ "$STATUS" == "200" ]]; then
              echo "Candidate revision $CANDIDATE_REVISION ($CANDIDATE_IMAGE) passed ingress smoke on attempt $attempt."
              echo "url=$ENDPOINT" >> "$GITHUB_OUTPUT"
              echo "revision=$CANDIDATE_REVISION" >> "$GITHUB_OUTPUT"
              HEALTHY=true
              break
            fi
            echo "Health check returned HTTP $STATUS on attempt $attempt; waiting for revision activation."
            sleep 10
          done
          if [[ "$HEALTHY" != true ]]; then
            echo "Health check did not return HTTP 200." >&2
            exit 1
          fi

      - name: Publish deployment summary
        if: always()
        env:
          ENDPOINT: ${{ steps.endpoint.outputs.url }}
          IMAGE: ${{ needs.prepare_release.outputs.image }}
          IMAGE_TAG: ${{ needs.prepare_release.outputs.image_tag }}
          PREVIOUS_IMAGE: ${{ needs.prepare_release.outputs.previous_image }}
          PREVIOUS_REVISION: ${{ needs.prepare_release.outputs.previous_revision }}
          REVISION: ${{ steps.candidate.outputs.revision }}
        run: |
          ENDPOINT="${ENDPOINT:-unavailable (deployment or smoke test failed)}"
          IMAGE="${IMAGE:-unavailable (preparation failed)}"
          IMAGE_TAG="${IMAGE_TAG:-unavailable (preparation failed)}"
          PREVIOUS_IMAGE="${PREVIOUS_IMAGE:-unavailable (inspection failed)}"
          PREVIOUS_REVISION="${PREVIOUS_REVISION:-unavailable (inspection failed)}"
          REVISION="${REVISION:-unavailable (deployment or smoke test failed)}"
          {
            echo "## CloudOrders deployment"
            echo
            printf -- '- Release: `%s`\n' "$GITHUB_SHA"
            printf -- '- Bicep deployment: `%s`\n' "$DEPLOYMENT_NAME"
            printf -- '- Immutable API image: `%s`\n' "$IMAGE"
            printf -- '- Published image tag: `%s`\n' "$IMAGE_TAG"
            printf -- '- Container App revision: `%s`\n' "$REVISION"
            printf -- '- Rollback image: `%s`\n' "$PREVIOUS_IMAGE"
            printf -- '- Previous ready revision: `%s`\n' "$PREVIOUS_REVISION"
            echo "- API endpoint: $ENDPOINT"
          } >> "$GITHUB_STEP_SUMMARY"
