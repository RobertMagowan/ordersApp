{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cloudorders.example/delivery/state.schema.json",
  "type": "object",
  "required": ["workflowVersion", "stateSchemaVersion", "configurationVersion", "workflowLifecycleOwner", "cutover", "sprintPlanSource", "sprintPlanRevision", "currentSprint"],
  "properties": {
    "workflowVersion": { "type": "string", "const": "2.0" },
    "stateSchemaVersion": { "type": "string", "const": "2.0" },
    "configurationVersion": { "type": "string", "const": "2.0" },
    "workflowLifecycleOwner": { "type": "string", "const": "sprint-orchestrator" },
    "cutover": {
      "type": "object",
      "required": ["status", "blockers"],
      "properties": {
        "status": { "enum": ["CUTOVER_BLOCKED", "WORKFLOW_CUTOVER_COMPLETE"] },
        "blockers": { "type": "array", "items": { "type": "string", "minLength": 1 } }
      },
      "additionalProperties": false
    },
    "sprintPlanSource": { "type": "string" },
    "sprintPlanRevision": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "currentSprint": {
      "type": "object",
      "required": ["id", "name", "requirementsAuthority", "workItems"],
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "requirementsAuthority": { "type": "array", "minItems": 1, "items": { "type": "string" } },
        "workItems": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/workItem" }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "$defs": {
    "gate": {
      "type": "object",
      "required": ["status"],
      "properties": {
        "status": { "enum": ["PENDING", "PASS", "FAIL", "NOT_APPLICABLE", "STALE", "HISTORICAL_UNVERIFIED", "BLOCKED"] }
      },
      "additionalProperties": false
    },
    "evidenceBinding": {
      "type": "object",
      "required": ["status"],
      "properties": {
        "commit": { "type": "string", "pattern": "^[0-9a-f]{7,40}$" },
        "workflowRun": { "type": "integer", "minimum": 1 },
        "environment": { "enum": ["development", "test", "production"] },
        "artifact": { "type": "string", "minLength": 1 },
        "status": { "enum": ["CURRENT", "STALE", "HISTORICAL_UNVERIFIED", "SUPERSEDED"] }
      },
      "additionalProperties": false
    },
    "workItem": {
      "type": "object",
      "required": ["id", "name", "risk", "lifecycle", "prLifecycle", "reviewStatus", "stage", "acceptanceVerification", "evidenceBindings", "retryCounters", "blockers", "gates"],
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "risk": { "enum": ["low", "standard", "high"] },
        "lifecycle": { "enum": ["TODO", "IN_PROGRESS", "MERGED", "DEV_DEPLOYED", "CANCELLED", "SUPERSEDED"] },
        "prLifecycle": { "enum": ["NONE", "DRAFT", "READY_FOR_REVIEW", "MERGED", "CLOSED"] },
        "reviewStatus": { "enum": ["NOT_REQUESTED", "PENDING", "CHANGES_REQUESTED", "APPROVED", "STALE", "HISTORICAL_UNVERIFIED", "NOT_APPLICABLE"] },
        "stage": { "enum": ["READINESS_CHECK", "IMPACT_ANALYSIS", "PLANNING", "PLAN_REVIEW", "IMPLEMENTATION", "LOCAL_VALIDATION", "PRE_REVIEW_CHECK", "PR_REVIEW", "FINAL_CI", "PROMOTION", "DEPLOYMENT", "DEV_VALIDATION", "SPRINT_INTEGRATION", "QA", "BLOCKED"] },
        "acceptanceVerification": {
          "type": "object",
          "required": ["stage", "status"],
          "properties": {
            "stage": { "enum": ["PRE_MERGE", "DEVELOPMENT", "SPRINT_INTEGRATION", "QA"] },
            "status": { "enum": ["PENDING", "PASS", "FAIL", "NOT_APPLICABLE", "STALE", "HISTORICAL_UNVERIFIED", "BLOCKED"] }
          },
          "additionalProperties": false
        },
        "evidenceBindings": { "type": "array", "items": { "$ref": "#/$defs/evidenceBinding" } },
        "retryCounters": {
          "type": "object",
          "required": ["command", "deployment"],
          "properties": {
            "command": { "type": "integer", "minimum": 0 },
            "deployment": { "type": "integer", "minimum": 0 }
          },
          "additionalProperties": false
        },
        "blockers": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["status", "reason"],
            "properties": {
              "status": { "enum": ["OPEN", "RESOLVED", "HUMAN_DECISION_REQUIRED"] },
              "reason": { "type": "string", "minLength": 1 }
            },
            "additionalProperties": false
          }
        },
        "gates": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/gate" }
        }
      },
      "additionalProperties": false
    }
  }
}
